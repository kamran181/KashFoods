import { Button } from "./ui/button"

const MainNav = () => {
  return (
    <div>
        <Button variant={"ghost"} className="text-bold hover:text-orange-500 hover:bg-white">
          Log In
        </Button>
    </div>
  )
}

export default MainNav